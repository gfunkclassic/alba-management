node_modules

# Firebase 서비스 계정 키 (절대 커밋 금지)
serviceAccountKey.json

# 빌드 산출물
dist

# 임시 관리자 스크립트 (운영 저장소에 포함 안 함)
*.cjs
scan_users.js
migrate_roles.js

# Firebase 캐시
.firebase

# Claude Code 로컬 설정 (환경 전용, 저장소 공유 불필요)
.claude/

# 일별 handoff 체크리스트 (로컬 운영 문서, 저장소 누적 방지)
tomorrow-checklist-*.md

# 테스트 데이터 정리 스크립트 (로컬 경로 하드코딩 포함, 공용 부적절)
functions/cleanup-test.js

# ─── 데이터 이전/백업 보호 (회사 GitHub 이전 대비) ───
# Local backup outputs
backups/
backup/
firestore-exports/

# Firebase / Google service account keys
*serviceAccount*.json
*service-account*.json
*-firebase-adminsdk-*.json
google-application-credentials*.json
firebase-admin-key*.json

# Environment variables / local secrets
.env
.env.*
!.env.example

# Local logs / temporary outputs
*.log
*.tmp
.DS_Store
Thumbs.db

# ─── 회사 GitHub 이전 보호: 개인정보 산출물 / 오실행 위험 스크립트 ───
# 직원 실명·근태·계약시간 등 PII 포함 감사 산출물 (절대 커밋 금지)
scripts/output/
# 과거 1회성 마이그레이션/write 스크립트 묶음 (운영 데이터 변경 위험)
scripts/archive-*/
# 운영 데이터 변경 가능 write 스크립트
scripts/cleanup-test-leave-data.mjs
scripts/backfill-external-employee-id.mjs
